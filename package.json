{
  "name": "youtube-subtitles-app",
  "version": "1.0.0",
  "description": "A full-stack application to display YouTube videos with AI-generated subtitles.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.19.2"
  }
}

